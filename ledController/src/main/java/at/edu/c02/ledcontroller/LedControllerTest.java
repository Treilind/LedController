package at.edu.c02.ledcontroller;

/*import org.json.JSONArray;
import org.json.JSONObject;
import org.junit.jupiter.api.BeforeEach;
import org.mockito.Mockito;

import java.io.IOException;
import java.util.ArrayList;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;*/

public class LedControllerTest {

   /* private ApiService apiService;
    private LedController ledController;

    @BeforeEach
    public void setUp() {
        apiService = Mockito.mock(ApiService.class);
        ledController = new LedControllerImpl(apiService);
    }

    public void testGetGroupLeds() throws IOException {

        when(apiService.getGroupLeds).thenReturn("fakewerte");
        ArrayList<JSONObject> result = ledController.getGroupLeds();
        verify(apiService).getGroupLeds();
        assertEquals("fakewerte", result, "Die zurückgegebenen Gruppen-LEDs stimmen mit den gefälschten Daten überein.");

    }*/
}